import { Wrapper } from './UserMenuStyled';

export const UserMenu = () => {
  return (
    <Wrapper>
      <span></span>
      <p></p>
      <button type="submit">Logout</button>
    </Wrapper>
  );
};
